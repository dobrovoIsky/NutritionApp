<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:viewModels="clr-namespace:NutritionApp.ViewModels"
             xmlns:models="clr-namespace:NutritionApp.Models"
             x:Class="NutritionApp.Views.MyRationPage"
             x:DataType="viewModels:MyRationViewModel"
             Title="Мій раціон"
             Shell.NavBarIsVisible="False"
             Background="{StaticResource PageBackgroundGradient}">

    <!-- Додаємо конвертер у ресурси -->
    <ContentPage.Resources>
        <ResourceDictionary>
            <local:InvertedBoolConverter x:Key="InvertedBoolConverter" xmlns:local="clr-namespace:NutritionApp.Converters" />
        </ResourceDictionary>
    </ContentPage.Resources>

    <Grid RowDefinitions="Auto,*" Padding="0">
        <!-- Header -->
        <Grid Row="0" Padding="0,16,0,16">
            <Grid ColumnDefinitions="Auto,*,Auto" Padding="16,0">
                <Border WidthRequest="40" HeightRequest="40" StrokeShape="RoundRectangle 20"
                        BackgroundColor="{StaticResource CardBackground}" Stroke="{StaticResource BorderColor}"
                        HorizontalOptions="Start">
                    <Border.GestureRecognizers>
                        <TapGestureRecognizer Command="{Binding GoBackCommand}" />
                    </Border.GestureRecognizers>
                    <Label Text="‹" FontSize="28" TextColor="{StaticResource TextPrimary}" VerticalOptions="Center" HorizontalOptions="Center"/>
                </Border>
                <Label Grid.Column="1" Text="Мій раціон" FontAttributes="Bold" FontSize="24" TextColor="{StaticResource TextPrimary}" HorizontalOptions="Center" VerticalOptions="Center"/>
            </Grid>
        </Grid>

        <!-- Content -->
        <Border Grid.Row="1"
                BackgroundColor="{StaticResource CardBackground}"
                Stroke="{StaticResource BorderColor}"
                StrokeThickness="1"
                Padding="16"
                Margin="16,0,16,16"
                StrokeShape="RoundRectangle 24">

            <Grid>
                <ActivityIndicator IsRunning="{Binding IsLoading}" IsVisible="{Binding IsLoading}" Color="{StaticResource Primary}" HorizontalOptions="Center" VerticalOptions="Center"/>

                <CollectionView ItemsSource="{Binding MealGroups}" IsGrouped="True" IsVisible="{Binding IsLoading, Converter={StaticResource InvertedBoolConverter}}">
                    <CollectionView.GroupHeaderTemplate>
                        <DataTemplate x:DataType="viewModels:MealGroup">
                            <Grid ColumnDefinitions="*,Auto" Padding="0,10,0,10">
                                <HorizontalStackLayout Spacing="8" VerticalOptions="Center">
                                    <Label Text="{Binding Name}" FontAttributes="Bold" FontSize="18" TextColor="{StaticResource TextPrimary}" />
                                    <Label Text="{Binding TotalCalories, StringFormat='({0} ккал)'}" FontSize="14" TextColor="{StaticResource TextSecondary}" VerticalOptions="Center"/>
                                </HorizontalStackLayout>
                                <Border Grid.Column="1" BackgroundColor="{StaticResource CardBackground}" WidthRequest="36" HeightRequest="36" StrokeShape="RoundRectangle 18" Stroke="{StaticResource Primary}">
                                    <Border.GestureRecognizers>
                                        <TapGestureRecognizer Command="{Binding Source={RelativeSource AncestorType={x:Type viewModels:MyRationViewModel}}, Path=AddFoodCommand}" CommandParameter="{Binding .}"/>
                                    </Border.GestureRecognizers>
                                    <Label Text="+" FontSize="24" TextColor="{StaticResource Primary}" HorizontalOptions="Center" VerticalOptions="Center" Margin="0,-2,0,0"/>
                                </Border>
                            </Grid>
                        </DataTemplate>
                    </CollectionView.GroupHeaderTemplate>

                    <CollectionView.ItemTemplate>
                        <DataTemplate x:DataType="models:FoodEntry">
                            <Border Stroke="{StaticResource BorderColor}"
                                    BackgroundColor="{StaticResource CardBackground}"
                                    StrokeThickness="1"
                                    StrokeShape="RoundRectangle 12"
                                    Padding="12"
                                    Margin="0,0,0,8">
                                <Grid ColumnDefinitions="*,Auto">
                                    <VerticalStackLayout>
                                        <Label Text="{Binding Name}" FontAttributes="Bold" FontSize="14" TextColor="{StaticResource TextPrimary}"/>
                                        <Label Text="{Binding Weight, StringFormat='{0} г'}" FontSize="12" TextColor="{StaticResource TextSecondary}"/>
                                    </VerticalStackLayout>
                                    <Label Grid.Column="1" Text="{Binding Calories, StringFormat='{0} ккал'}" FontAttributes="Bold" FontSize="14" TextColor="{StaticResource Primary}" VerticalOptions="Center"/>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </CollectionView.ItemTemplate>

                    <CollectionView.GroupFooterTemplate>
                        <DataTemplate>
                            <BoxView HeightRequest="1" Color="{StaticResource BorderColor}" Margin="0,5,0,15"/>
                        </DataTemplate>
                    </CollectionView.GroupFooterTemplate>
                </CollectionView>
            </Grid>
        </Border>
    </Grid>
</ContentPage>
