<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:viewmodels="clr-namespace:NutritionApp.ViewModels"
             xmlns:converters="clr-namespace:NutritionApp.Converters"
             x:Class="NutritionApp.Views.LeaderboardPage"
             BackgroundImageSource="app_background_img.png"
             Title="Рейтинг користувачів">

    <ContentPage.Resources>
        <converters:Base64ToImageSourceConverter x:Key="Base64Converter" />
    </ContentPage.Resources>

    <Grid RowDefinitions="Auto, *">
        <!-- Заголовок -->
        <Border Grid.Row="0" Margin="20,20,20,10" Style="{StaticResource ActiveGlowBorder}">
            <VerticalStackLayout Padding="20">
                <Label Text="🏆 Лідерборд" Style="{StaticResource TitleLabel}" HorizontalOptions="Center" />
                <Label Text="Топ 50 користувачів за балами" TextColor="{StaticResource TextSecondary}" HorizontalOptions="Center" Margin="0,5,0,0"/>
            </VerticalStackLayout>
        </Border>

        <RefreshView Grid.Row="1" Command="{Binding RefreshLeaderboardCommand}" IsRefreshing="{Binding IsLoading}">
            <CollectionView ItemsSource="{Binding LeaderboardUsers}" Margin="20,0,20,20">
                <CollectionView.ItemTemplate>
                    <DataTemplate>
                        <Border Margin="0,5" StrokeThickness="0" BackgroundColor="{StaticResource CardBackground}" StrokeShape="RoundRectangle 15">
                            <Grid Padding="15" ColumnDefinitions="Auto, Auto, *, Auto">
                                <!-- Місце -->
                                <Label Grid.Column="0" Text="{Binding Rank}" VerticalOptions="Center" FontSize="18" FontAttributes="Bold" TextColor="{StaticResource TextSecondary}" WidthRequest="30" />
                                
                                <!-- Аватар -->
                                <Border Grid.Column="1" WidthRequest="60" HeightRequest="60" StrokeShape="RoundRectangle 30" StrokeThickness="0" Margin="10,0" BackgroundColor="{StaticResource AccentColor}">
                                    <Grid>
                                        <Label Text="{Binding AvatarChar}" IsVisible="{Binding HasAvatar, Converter={StaticResource InvertedBoolConverter}}" VerticalOptions="Center" HorizontalOptions="Center" HorizontalTextAlignment="Center" VerticalTextAlignment="Center" TextColor="White" FontAttributes="Bold" FontSize="26" />
                                        <Image WidthRequest="60" HeightRequest="60" Source="{Binding User.AvatarBase64, Converter={StaticResource Base64Converter}}" IsVisible="{Binding HasAvatar}" Aspect="AspectFill" HorizontalOptions="Fill" VerticalOptions="Fill" />
                                    </Grid>
                                </Border>
                                
                                <!-- Інфо -->
                                <VerticalStackLayout Grid.Column="2" VerticalOptions="Center">
                                    <Label Text="{Binding User.Username}" FontAttributes="Bold" TextColor="{StaticResource TextPrimary}" FontSize="16" />
                                    <HorizontalStackLayout Spacing="4">
                                        <Image Source="xp.png" WidthRequest="20" HeightRequest="20" VerticalOptions="Center" />
                                        <Label Text="{Binding User.MonthlyPoints, StringFormat='{0} XP'}" TextColor="{StaticResource AccentColor}" FontSize="14" VerticalOptions="Center" />
                                    </HorizontalStackLayout>
                                </VerticalStackLayout>
                                
                                <!-- Стрік -->
                                <HorizontalStackLayout Grid.Column="3" VerticalOptions="Center" Spacing="5">
                                    <Image Source="strik.png" WidthRequest="20" HeightRequest="20" VerticalOptions="Center"/>
                                    <Label Text="{Binding User.CurrentStreak}" TextColor="{StaticResource TextPrimary}" FontAttributes="Bold" FontSize="16" VerticalOptions="Center" />
                                </HorizontalStackLayout>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
                <CollectionView.EmptyView>
                    <VerticalStackLayout HorizontalOptions="Center" VerticalOptions="Center">
                        <Label Text="Завантаження рейтингу..." TextColor="{StaticResource TextSecondary}" HorizontalOptions="Center" />
                    </VerticalStackLayout>
                </CollectionView.EmptyView>
            </CollectionView>
        </RefreshView>
    </Grid>
</ContentPage>
