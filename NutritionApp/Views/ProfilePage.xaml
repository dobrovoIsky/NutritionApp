<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="NutritionApp.Views.ProfilePage"
             Shell.NavBarIsVisible="False"
             Background="{StaticResource PageBackgroundGradient}">

    <Grid RowDefinitions="260,*" Padding="0">

        <!-- Верхній блок з градієнтом -->
        <Grid Row="0" Padding="0,16,0,16">


            <VerticalStackLayout Spacing="14"
                                 VerticalOptions="Center"
                                 HorizontalOptions="Center">
                <!-- Аватар у кругу зверху - тепер натискається -->
                <Border HeightRequest="104"
                        WidthRequest="104"
                        Stroke="White"
                        StrokeThickness="3"
                        StrokeShape="RoundRectangle 52"
                        BackgroundColor="#FFFFFF22"
                        HorizontalOptions="Center">
                    <Border.Shadow>
                        <Shadow Brush="Black" Offset="0,4" Radius="10" Opacity="0.25"/>
                    </Border.Shadow>
                    <Border.GestureRecognizers>
                        <TapGestureRecognizer Tapped="OnAvatarTapped"/>
                    </Border.GestureRecognizers>
                    <Image x:Name="AvatarImage"
                           Source="{Binding SelectedAvatar.ImageSource}"
                           HeightRequest="100"
                           WidthRequest="100"
                           Aspect="AspectFill">
                         <Image.Clip>
                             <EllipseGeometry Center="50,50" RadiusX="50" RadiusY="50"/>
                         </Image.Clip>
                    </Image>
                </Border>

                <!-- Підказка -->
                <Label Text="(натисни для зміни аватара)"
                       FontSize="11"
                       TextColor="#FFFFFFAA"
                       HorizontalOptions="Center"/>

                <Label x:Name="UsernameLabel"
                       Text="{Binding UserProfile.Username, StringFormat='Вітаємо, {0}!'}"
                       FontAttributes="Bold"
                       FontSize="22"
                       TextColor="White"
                       HorizontalOptions="Center"/>

                <Label Text="Твій профіль та показники"
                       FontSize="14"
                       TextColor="#FFEFF2"
                       HorizontalOptions="Center"/>
            </VerticalStackLayout>
        </Grid>

        <!-- Контент -->
        <Border Grid.Row="1"
                BackgroundColor="{StaticResource CardBackground}"
                Stroke="{StaticResource BorderColor}"
                StrokeThickness="1"
                Padding="20"
                Margin="16,-32,16,0"
                StrokeShape="RoundRectangle 24">

            <ScrollView>
                <VerticalStackLayout Spacing="18">

                    <!-- Кнопки дій -->
                    <Grid ColumnDefinitions="*,*"
                          ColumnSpacing="10">
                        <Button Text="Edit profile"
                                Clicked="OnEditProfileClicked"
                                HeightRequest="48"
                                CornerRadius="14"
                                BackgroundColor="{StaticResource Primary}"
                                TextColor="White"
                                FontAttributes="Bold"/>

                        <Button Grid.Column="1"
                                Text="Logout"
                                Clicked="OnLogoutButtonClicked"
                                HeightRequest="48"
                                CornerRadius="14"
                                BackgroundColor="Transparent"
                                TextColor="{StaticResource Primary}"
                                BorderColor="{StaticResource Primary}"
                                BorderWidth="1.2"/>
                    </Grid>

                    <!-- Блок з основними даними -->
                    <VerticalStackLayout Spacing="12">
                        <Label Text="Мої показники"
                               FontSize="18"
                               FontAttributes="Bold"
                               TextColor="{StaticResource TextPrimary}"/>

                        <Grid ColumnDefinitions="*,*"
                              RowSpacing="12"
                              ColumnSpacing="12">

                            <Border Stroke="{StaticResource BorderColor}"
                                    BackgroundColor="{StaticResource CardBackground}"
                                    StrokeThickness="1"
                                    StrokeShape="RoundRectangle 14"
                                    Padding="12">
                                <VerticalStackLayout Spacing="4">
                                    <Label Text="Height"
                                           FontSize="13"
                                           TextColor="{StaticResource TextSecondary}"/>
                                    <Label Text="{Binding UserProfile.Height, StringFormat='{0:F1} см'}"
                                           FontSize="18"
                                           FontAttributes="Bold"
                                           TextColor="{StaticResource TextPrimary}"/>
                                </VerticalStackLayout>
                            </Border>

                            <Border Grid.Column="1"
                                    Stroke="{StaticResource BorderColor}"
                                    BackgroundColor="{StaticResource CardBackground}"
                                    StrokeThickness="1"
                                    StrokeShape="RoundRectangle 14"
                                    Padding="12">
                                <VerticalStackLayout Spacing="4">
                                    <Label Text="Weight"
                                           FontSize="13"
                                           TextColor="{StaticResource TextSecondary}"/>
                                    <Label Text="{Binding UserProfile.Weight, StringFormat='{0:F1} кг'}"
                                           FontSize="18"
                                           FontAttributes="Bold"
                                           TextColor="{StaticResource TextPrimary}"/>
                                </VerticalStackLayout>
                            </Border>

                            <Border Grid.Row="1"
                                    Stroke="{StaticResource BorderColor}"
                                    BackgroundColor="{StaticResource CardBackground}"
                                    StrokeThickness="1"
                                    StrokeShape="RoundRectangle 14"
                                    Padding="12">
                                <VerticalStackLayout Spacing="4">
                                    <Label Text="Age"
                                           FontSize="13"
                                           TextColor="{StaticResource TextSecondary}"/>
                                    <Label Text="{Binding UserProfile.Age, StringFormat='{0} років'}"
                                           FontSize="18"
                                           FontAttributes="Bold"
                                           TextColor="{StaticResource TextPrimary}"/>
                                </VerticalStackLayout>
                            </Border>

                            <Border Grid.Row="1" Grid.Column="1"
                                    Stroke="{StaticResource BorderColor}"
                                    BackgroundColor="{StaticResource CardBackground}"
                                    StrokeThickness="1"
                                    StrokeShape="RoundRectangle 14"
                                    Padding="12">
                                <VerticalStackLayout Spacing="4">
                                    <Label Text="Activity"
                                           FontSize="13"
                                           TextColor="{StaticResource TextSecondary}"/>
                                    <Label Text="{Binding UserProfile.ActivityLevel}"
                                           FontSize="16"
                                           FontAttributes="Bold"
                                           TextColor="{StaticResource TextPrimary}"
                                           LineBreakMode="TailTruncation"/>
                                </VerticalStackLayout>
                            </Border>

                            <Border Grid.Row="2" Grid.ColumnSpan="2"
                                    Stroke="{StaticResource BorderColor}"
                                    BackgroundColor="{StaticResource CardBackground}"
                                    StrokeThickness="1"
                                    StrokeShape="RoundRectangle 14"
                                    Padding="12">
                                <VerticalStackLayout Spacing="4">
                                    <Label Text="Goal"
                                           FontSize="13"
                                           TextColor="{StaticResource TextSecondary}"/>
                                    <Label Text="{Binding UserProfile.Goal}"
                                           FontSize="18"
                                           FontAttributes="Bold"
                                           TextColor="{StaticResource TextPrimary}"/>
                                </VerticalStackLayout>
                            </Border>
                        </Grid>
                    </VerticalStackLayout>

                    <!-- Налаштування -->
                    <Label Text="Налаштування"
                           FontSize="18"
                           FontAttributes="Bold"
                           TextColor="{StaticResource TextPrimary}"
                           Margin="0,8,0,0"/>

                    <!-- Нагадування про воду -->
                    <Border Stroke="{StaticResource BorderColor}"
                            BackgroundColor="{StaticResource CardBackground}"
                            StrokeThickness="1"
                            StrokeShape="RoundRectangle 14"
                            Padding="14">
                        <VerticalStackLayout Spacing="12">
                            <Grid ColumnDefinitions="Auto,*,Auto" VerticalOptions="Center">
                                <Border WidthRequest="40" 
                                        HeightRequest="40"
                                        StrokeShape="RoundRectangle 20"
                                        BackgroundColor="#1E3A8A"
                                        Stroke="Transparent">
                                    <Label Text="💧" 
                                           FontSize="18" 
                                           VerticalOptions="Center" 
                                           HorizontalOptions="Center"/>
                                </Border>
                                <VerticalStackLayout Grid.Column="1" Margin="12,0,0,0" VerticalOptions="Center">
                                    <Label Text="Нагадування про воду"
                                           FontSize="15"
                                           FontAttributes="Bold"
                                           TextColor="{StaticResource TextPrimary}"/>
                                    <Label Text="Регулярні сповіщення пити воду"
                                           FontSize="12"
                                           TextColor="{StaticResource TextSecondary}"/>
                                </VerticalStackLayout>
                                <Switch x:Name="WaterReminderSwitch"
                                        Grid.Column="2"
                                        Toggled="OnWaterReminderToggled"
                                        VerticalOptions="Center"/>
                            </Grid>

                            <!-- Вибір інтервалу (показується тільки коли включено) -->
                            <Border x:Name="IntervalPickerContainer"
                                    IsVisible="False"
                                    Stroke="{StaticResource BorderColor}"
                                    StrokeThickness="1"
                                    StrokeShape="RoundRectangle 10"
                                    BackgroundColor="{StaticResource PageBackground}"
                                    Padding="10">
                                <VerticalStackLayout Spacing="10">
                                    <Picker x:Name="IntervalPicker"
                                            Title="Інтервал нагадувань"
                                            SelectedIndexChanged="OnIntervalChanged"
                                            TextColor="{StaticResource TextPrimary}"
                                            TitleColor="{StaticResource TextSecondary}">
                                        <Picker.ItemsSource>
                                            <x:Array Type="{x:Type x:String}">
                                                <x:String>Кожні 30 хвилин</x:String>
                                                <x:String>Кожні 45 хвилин</x:String>
                                                <x:String>Кожні 60 хвилин</x:String>
                                                <x:String>Кожні 90 хвилин</x:String>
                                                <x:String>Кожні 2 години</x:String>
                                            </x:Array>
                                        </Picker.ItemsSource>
                                    </Picker>

                                    <!-- Вибір часу: Від - До -->
                                    <Grid ColumnDefinitions="*,Auto,*" ColumnSpacing="12" Margin="0,8,0,0">
                                        <VerticalStackLayout Spacing="4">
                                            <Label Text="Start Time" FontSize="12" TextColor="{StaticResource TextSecondary}"/>
                                            <Border Stroke="{StaticResource BorderColor}" 
                                                    StrokeShape="RoundRectangle 8"
                                                    BackgroundColor="{StaticResource PageBackground}"
                                                    Padding="8,0"
                                                    HeightRequest="40">
                                                <TimePicker Time="{Binding WaterStartTime}" 
                                                            Format="HH:mm" 
                                                            TextColor="{StaticResource TextPrimary}"
                                                            VerticalOptions="Center"/>
                                            </Border>
                                        </VerticalStackLayout>
                                        
                                        <Label Text="—" 
                                               Grid.Column="1" 
                                               VerticalOptions="Center" 
                                               Margin="0,18,0,0"
                                               TextColor="{StaticResource TextSecondary}"/>

                                        <VerticalStackLayout Grid.Column="2" Spacing="4">
                                            <Label Text="End Time" FontSize="12" TextColor="{StaticResource TextSecondary}"/>
                                            <Border Stroke="{StaticResource BorderColor}" 
                                                    StrokeShape="RoundRectangle 8"
                                                    BackgroundColor="{StaticResource PageBackground}"
                                                    Padding="8,0"
                                                    HeightRequest="40">
                                                <TimePicker Time="{Binding WaterEndTime}" 
                                                            Format="HH:mm" 
                                                            TextColor="{StaticResource TextPrimary}"
                                                            VerticalOptions="Center"/>
                                            </Border>
                                        </VerticalStackLayout>
                                    </Grid>
                                </VerticalStackLayout>
                            </Border>
                        </VerticalStackLayout>
                    </Border>


                </VerticalStackLayout>
            </ScrollView>
        </Border>

        <!-- Popup для вибору аватара -->
        <Grid x:Name="AvatarPopup" 
              Grid.RowSpan="2"
              IsVisible="False"
              BackgroundColor="#80000000"
              ZIndex="100">
            <Grid.GestureRecognizers>
                <TapGestureRecognizer Tapped="OnClosePopup"/>
            </Grid.GestureRecognizers>

            <Border VerticalOptions="Center"
                    HorizontalOptions="Center"
                    BackgroundColor="{StaticResource CardBackground}"
                    Stroke="{StaticResource BorderColor}"
                    StrokeThickness="1"
                    StrokeShape="RoundRectangle 24"
                    Padding="24"
                    WidthRequest="300">
                <Border.GestureRecognizers>
                    <TapGestureRecognizer/>
                </Border.GestureRecognizers>

                <VerticalStackLayout Spacing="16">
                    <Label Text="Обери аватар"
                           FontSize="20"
                           FontAttributes="Bold"
                           TextColor="{StaticResource TextPrimary}"
                           HorizontalOptions="Center"/>

                    <CollectionView ItemsSource="{Binding AvatarOptions}"
                                    SelectionMode="Single"
                                    SelectedItem="{Binding SelectedAvatar}"
                                    SelectionChanged="OnAvatarSelected"
                                    HorizontalOptions="Center"
                                    HeightRequest="160">
                        <CollectionView.ItemsLayout>
                            <GridItemsLayout Orientation="Vertical" 
                                             Span="4"
                                             HorizontalItemSpacing="8"
                                             VerticalItemSpacing="8"/>
                        </CollectionView.ItemsLayout>
                        <CollectionView.ItemTemplate>
                            <DataTemplate>
                                <Border HeightRequest="70"
                                        WidthRequest="70"
                                        StrokeThickness="3"
                                        StrokeShape="RoundRectangle 35"
                                        BackgroundColor="#FFFFFF11"
                                        HorizontalOptions="Center"
                                        VerticalOptions="Center"
                                        Margin="4">
                                    <Border.Style>
                                        <Style TargetType="Border">
                                            <Setter Property="Stroke" Value="Transparent"/>
                                            <Style.Triggers>
                                                <DataTrigger TargetType="Border" Binding="{Binding IsSelected}" Value="True">
                                                    <Setter Property="Stroke" Value="{StaticResource Primary}"/>
                                                    <Setter Property="Scale" Value="1.1"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Border.Style>
                                    
                                    <Grid>
                                        <Image Source="{Binding ImageSource}"
                                               HeightRequest="64"
                                               WidthRequest="64"
                                               Aspect="AspectFill"
                                               HorizontalOptions="Center"
                                               VerticalOptions="Center">
                                            <Image.Clip>
                                                <EllipseGeometry Center="32,32" RadiusX="32" RadiusY="32"/>
                                            </Image.Clip>
                                        </Image>
                                        
                                        <!-- Checkmark overlay for selected item -->
                                        <Border IsVisible="{Binding IsSelected}"
                                                BackgroundColor="{StaticResource Primary}"
                                                HorizontalOptions="End"
                                                VerticalOptions="End"
                                                StrokeShape="RoundRectangle 10"
                                                HeightRequest="20"
                                                WidthRequest="20"
                                                Stroke="White"
                                                StrokeThickness="1"
                                                Margin="0,0,0,0">
                                            <Label Text="✓" 
                                                   TextColor="White" 
                                                   FontSize="12" 
                                                   FontAttributes="Bold"
                                                   HorizontalOptions="Center" 
                                                   VerticalOptions="Center"
                                                   TranslationY="-1"/>
                                        </Border>
                                    </Grid>
                                </Border>
                            </DataTemplate>
                        </CollectionView.ItemTemplate>
                    </CollectionView>

                    <Button Text="Закрити"
                            BackgroundColor="{StaticResource Primary}"
                            TextColor="White"
                            CornerRadius="12"
                            HeightRequest="44"
                            Clicked="OnClosePopup"/>
                </VerticalStackLayout>
            </Border>
        </Grid>

    </Grid>
</ContentPage>