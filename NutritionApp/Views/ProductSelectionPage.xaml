<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:viewmodels="clr-namespace:NutritionApp.ViewModels"
             xmlns:models="clr-namespace:NutritionApp.Models"
             x:Class="NutritionApp.Views.ProductSelectionPage"
             x:Name="ThisPage"
             x:DataType="viewmodels:ProductSelectionViewModel"
             Shell.NavBarIsVisible="False"
             Background="{StaticResource PageBackgroundGradient}">
    <ContentPage.Resources>
        <local:BoolToColorConverter x:Key="BoolToColorConverter" xmlns:local="clr-namespace:NutritionApp.Converters"/>
    </ContentPage.Resources>

    <Grid RowDefinitions="Auto,Auto,*,Auto" Padding="16,20,16,0">

        <!-- Заголовок -->
        <Grid Row="0" ColumnDefinitions="Auto,*,Auto" Margin="0,0,0,16">
            <Border WidthRequest="40" HeightRequest="40" StrokeShape="RoundRectangle 20"
                    BackgroundColor="#FFFFFF22" Stroke="White" StrokeThickness="1"
                    HorizontalOptions="Start" VerticalOptions="Center" Margin="0,0,16,0">
                <Border.GestureRecognizers>
                    <TapGestureRecognizer Tapped="OnBackClicked"/>
                </Border.GestureRecognizers>
                <Label Text="‹" FontSize="28" TextColor="White" VerticalOptions="Center" HorizontalOptions="Center" TranslationY="-2"/>
            </Border>

            <VerticalStackLayout Grid.Column="1" VerticalOptions="Center">
                <Label Text="{Binding SelectedCount, StringFormat='Обрано: {0}'}" FontSize="14" TextColor="{StaticResource TextSecondary}" />
                <Label Text="Наявні продукти" FontAttributes="Bold" FontSize="26" TextColor="{StaticResource TextPrimary}" />
            </VerticalStackLayout>

            <Border Grid.Column="2" BackgroundColor="#4DFFFFFF" StrokeShape="RoundRectangle 12" Stroke="Transparent" Padding="12,10" VerticalOptions="Center">
                <Border.GestureRecognizers>
                    <TapGestureRecognizer Command="{Binding ClearAllCommand}"/>
                </Border.GestureRecognizers>
                <Label Text="Очистити" FontSize="14" FontAttributes="Bold" TextColor="White"/>
            </Border>
        </Grid>

        <!-- Пошук та категорії -->
        <VerticalStackLayout Grid.Row="1" Spacing="16" Margin="0,0,0,16">
            
            <Border Style="{StaticResource ActiveGlowBorder}" BackgroundColor="{StaticResource CardBackground}" Padding="16,8">
                <Grid ColumnDefinitions="Auto,*">
                    <Label Text="🔍" FontSize="18" VerticalOptions="Center" Margin="0,0,10,0"/>
                    <Entry Grid.Column="1" Placeholder="Пошук продуктів..." Text="{Binding SearchText}" FontSize="16" TextColor="{StaticResource TextPrimary}" PlaceholderColor="{StaticResource TextSecondary}" BackgroundColor="Transparent"/>
                </Grid>
            </Border>

            <ScrollView Orientation="Horizontal" HorizontalScrollBarVisibility="Never">
                <HorizontalStackLayout Spacing="8" BindableLayout.ItemsSource="{Binding Categories}">
                    <BindableLayout.ItemTemplate>
                        <DataTemplate x:DataType="x:String">
                            <Border Style="{StaticResource ActiveGlowBorder}" BackgroundColor="{StaticResource CardBackground}" Padding="16,10">
                                <Border.GestureRecognizers>
                                    <TapGestureRecognizer Command="{Binding Source={x:Reference ThisPage}, Path=BindingContext.SelectCategoryCommand}" CommandParameter="{Binding}"/>
                                </Border.GestureRecognizers>
                                <Label Text="{Binding}" FontSize="14" FontAttributes="Bold" TextColor="{StaticResource TextPrimary}"/>
                            </Border>
                        </DataTemplate>
                    </BindableLayout.ItemTemplate>
                </HorizontalStackLayout>
            </ScrollView>
        </VerticalStackLayout>

        <!-- Список продуктів -->
        <CollectionView Grid.Row="2" ItemsSource="{Binding FilteredProducts}" SelectionMode="None" RemainingItemsThreshold="5" RemainingItemsThresholdReachedCommand="{Binding LoadMoreCommand}">
            <CollectionView.ItemsLayout>
                <LinearItemsLayout Orientation="Vertical" ItemSpacing="12"/>
            </CollectionView.ItemsLayout>
            <CollectionView.ItemTemplate>
                <DataTemplate x:DataType="models:ProductItem">
                    <Border Style="{StaticResource ActiveGlowBorder}" BackgroundColor="{StaticResource CardBackground}" Padding="16">
                        <Border.GestureRecognizers>
                            <TapGestureRecognizer Command="{Binding Source={x:Reference ThisPage}, Path=BindingContext.ToggleProductCommand}" CommandParameter="{Binding}"/>
                        </Border.GestureRecognizers>
                        
                        <Grid ColumnDefinitions="Auto,*,Auto">
                            <Label Text="{Binding Emoji}" FontSize="28" VerticalOptions="Center" Margin="0,0,16,0"/>
                            
                            <VerticalStackLayout Grid.Column="1" Spacing="4" VerticalOptions="Center">
                                <Label Text="{Binding Name}" FontSize="16" FontAttributes="Bold" TextColor="{StaticResource TextPrimary}"/>
                                <Label Text="{Binding Category}" FontSize="13" TextColor="{StaticResource TextSecondary}"/>
                            </VerticalStackLayout>
                            
                            <Frame Grid.Column="2" BackgroundColor="{Binding IsSelected, Converter={StaticResource BoolToColorConverter}}" CornerRadius="12" BorderColor="{StaticResource BorderColor}" WidthRequest="36" HeightRequest="36" Padding="0" HasShadow="False" VerticalOptions="Center">
                                <Label Text="✓" FontSize="20" FontAttributes="Bold" TextColor="White" IsVisible="{Binding IsSelected}" HorizontalOptions="Center" VerticalOptions="Center"/>
                            </Frame>
                        </Grid>
                    </Border>
                </DataTemplate>
            </CollectionView.ItemTemplate>
            
            <CollectionView.EmptyView>
                <VerticalStackLayout Spacing="16" VerticalOptions="Center" HorizontalOptions="Center" Margin="0,60">
                    <Label Text="🔍" FontSize="48" HorizontalOptions="Center"/>
                    <Label Text="Продуктів не знайдено" FontSize="16" FontAttributes="Bold" TextColor="{StaticResource TextSecondary}" HorizontalOptions="Center"/>
                </VerticalStackLayout>
            </CollectionView.EmptyView>
        </CollectionView>

        <!-- Нижня панель з кнопками -->
        <Grid Grid.Row="3" ColumnDefinitions="*,2*" ColumnSpacing="16" Padding="0,16,0,20">
            <Border Style="{StaticResource ActiveGlowBorder}" BackgroundColor="{StaticResource CardBackground}" Padding="0">
                <Border.GestureRecognizers>
                    <TapGestureRecognizer Command="{Binding AddCustomProductCommand}"/>
                </Border.GestureRecognizers>
                <Label Text="+ Свій" FontSize="16" FontAttributes="Bold" TextColor="{StaticResource TextPrimary}" HorizontalOptions="Center" VerticalOptions="Center"/>
            </Border>
            
            <Button Grid.Column="1" Text="✓ Підтвердити вибір" Clicked="OnConfirmClicked" Style="{StaticResource AccentButton}" HeightRequest="56" CornerRadius="28" FontAttributes="Bold" FontSize="18"/>
        </Grid>

    </Grid>
</ContentPage>
